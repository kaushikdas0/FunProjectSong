---
phase: 03-ai-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - .gitignore
  - src/vite-env.d.ts
  - src/lib/firebase.ts
  - src/hooks/useCompliment.ts
  - src/hooks/useCompliment.test.ts
  - vitest.config.ts
  - package.json
autonomous: true
requirements: [GEN-02, GEN-04, GEN-05]
user_setup:
  - service: firebase
    why: "Firebase AI Logic proxies Gemini API calls — project must exist before code runs"
    env_vars:
      - name: VITE_FIREBASE_API_KEY
        source: "Firebase Console -> Project Settings -> General -> Web app -> apiKey"
      - name: VITE_FIREBASE_AUTH_DOMAIN
        source: "Firebase Console -> Project Settings -> General -> Web app -> authDomain"
      - name: VITE_FIREBASE_PROJECT_ID
        source: "Firebase Console -> Project Settings -> General -> Web app -> projectId"
      - name: VITE_FIREBASE_STORAGE_BUCKET
        source: "Firebase Console -> Project Settings -> General -> Web app -> storageBucket"
      - name: VITE_FIREBASE_MESSAGING_SENDER_ID
        source: "Firebase Console -> Project Settings -> General -> Web app -> messagingSenderId"
      - name: VITE_FIREBASE_APP_ID
        source: "Firebase Console -> Project Settings -> General -> Web app -> appId"
    dashboard_config:
      - task: "Create Firebase project and register a web app"
        location: "Firebase Console -> Add project -> Add app (web)"
      - task: "Enable AI Logic (Gemini API)"
        location: "Firebase Console -> AI Logic -> Get started -> Enable"

must_haves:
  truths:
    - "useCompliment hook transitions through idle -> generating -> result states on successful generate"
    - "useCompliment hook transitions to error state when API call fails"
    - "Calling generate() again from result state produces a new compliment (regenerate)"
    - "Double-tapping generate is prevented by debounce ref guard"
    - "Firebase app initializes once as singleton — no duplicate app errors"
    - "Model name is fetched from Remote Config with fallback to default"
  artifacts:
    - path: "src/lib/firebase.ts"
      provides: "Firebase singleton init, AI instance, Remote Config model name fetch, createModel factory"
      exports: ["firebaseApp", "getModelName", "createModel"]
    - path: "src/hooks/useCompliment.ts"
      provides: "Four-state machine hook: idle/generating/result/error"
      exports: ["useCompliment", "ComplimentState"]
    - path: "src/hooks/useCompliment.test.ts"
      provides: "Unit tests for hook state transitions with mocked firebase/ai"
      min_lines: 50
    - path: "vitest.config.ts"
      provides: "Vitest configuration with jsdom for React"
    - path: ".env.example"
      provides: "Template for Firebase env vars (no real values)"
  key_links:
    - from: "src/hooks/useCompliment.ts"
      to: "src/lib/firebase.ts"
      via: "import { getModelName, createModel }"
      pattern: "import.*from.*lib/firebase"
    - from: "src/lib/firebase.ts"
      to: "firebase/ai"
      via: "getAI, getGenerativeModel, GoogleAIBackend"
      pattern: "GoogleAIBackend"
    - from: "src/lib/firebase.ts"
      to: "firebase/remote-config"
      via: "getRemoteConfig, fetchAndActivate, getValue"
      pattern: "fetchAndActivate"
---

<objective>
Wire Firebase AI Logic SDK and build the useCompliment hook with full state machine, error handling, and regenerate support. Set up test infrastructure (Wave 0).

Purpose: This is the AI backbone — everything in Phase 3 (and Phase 4) depends on this hook working correctly. Firebase singleton ensures no duplicate init; Remote Config enables model swaps without deploy.

Output: Working useCompliment hook (tested), Firebase singleton module, Vitest configured, .env.example for onboarding.
</objective>

<execution_context>
@/Volumes/x10/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/x10/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-foundation/03-CONTEXT.md
@.planning/phases/03-ai-foundation/03-RESEARCH.md
@.planning/phases/03-ai-foundation/03-VALIDATION.md

<interfaces>
<!-- Existing ComplimentCard props — useCompliment must produce data matching this shape -->
From src/components/Card/ComplimentCard.tsx:
```typescript
interface ComplimentCardProps {
  name: string;
  compliment: string;
}
```

<!-- Existing vite config — vitest.config.ts should extend or coexist -->
From vite.config.ts:
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import tailwindcss from '@tailwindcss/vite';

export default defineConfig({
  plugins: [react(), tailwindcss()],
});
```

<!-- No existing .env, lib/, or hooks/ directories — all created fresh -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Install dependencies and configure Vitest + env scaffolding</name>
  <files>package.json, vitest.config.ts, .env.example, .gitignore, src/vite-env.d.ts</files>
  <action>
1. Install firebase and test dependencies:
   ```
   npm install firebase
   npm install -D vitest @testing-library/react @testing-library/user-event jsdom
   ```

2. Create `vitest.config.ts` at project root:
   - Use `defineConfig` from `vitest/config`
   - Set `test.environment: 'jsdom'`
   - Set `test.globals: true`
   - Include the react plugin from `@vitejs/plugin-react` (same as vite.config.ts)
   - Include the tailwindcss plugin from `@tailwindcss/vite`

3. Create `.env.example` with all six VITE_FIREBASE_ keys as empty placeholders (no real values):
   ```
   VITE_FIREBASE_API_KEY=
   VITE_FIREBASE_AUTH_DOMAIN=
   VITE_FIREBASE_PROJECT_ID=
   VITE_FIREBASE_STORAGE_BUCKET=
   VITE_FIREBASE_MESSAGING_SENDER_ID=
   VITE_FIREBASE_APP_ID=
   ```

4. Update `.gitignore`: Add `.env` and `.env.local` lines if not already present (`.env.example` should NOT be gitignored).

5. Create or update `src/vite-env.d.ts`:
   - Keep the existing `/// <reference types="vite/client" />` line
   - Add `ImportMetaEnv` interface with all six `readonly VITE_FIREBASE_*: string` properties
   - Add `ImportMeta` interface extending `env` to `ImportMetaEnv`

6. Add `"test": "vitest run --reporter=verbose"` to package.json scripts.
  </action>
  <verify>
    <automated>cd /Volumes/x10/CODE/FunProjectSong && npx vitest run --reporter=verbose 2>&1 | head -20</automated>
  </verify>
  <done>Vitest runs (0 tests, no errors). Firebase package in node_modules. .env.example lists all six keys. .gitignore blocks .env. TypeScript recognizes VITE_FIREBASE_* env vars.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Firebase singleton module + useCompliment hook with tests</name>
  <files>src/lib/firebase.ts, src/hooks/useCompliment.ts, src/hooks/useCompliment.test.ts</files>
  <behavior>
    - Test: initial state is { status: "idle" }
    - Test: calling generate("Alice") transitions to { status: "generating" } then { status: "result", name: "Alice", compliment: "mocked compliment" }
    - Test: calling generate() when already generating (isFlyingRef=true) is a no-op (debounce)
    - Test: when generateContent throws, state transitions to { status: "error" }
    - Test: calling generate("Bob") from error state transitions back through generating to result
    - Test: calling generate("Carol") from result state (regenerate) produces new result
  </behavior>
  <action>
**RED first** — Write tests in `src/hooks/useCompliment.test.ts`:
- Mock `firebase/ai` module: `getAI` returns mock, `getGenerativeModel` returns object with `generateContent` that resolves `{ response: { text: () => "mocked compliment" } }`, `GoogleAIBackend` is a passthrough class.
- Mock `firebase/app`: `initializeApp` returns mock app object.
- Mock `firebase/remote-config`: `getRemoteConfig` returns mock, `fetchAndActivate` resolves, `getValue` returns `{ asString: () => "gemini-2.5-flash" }`.
- Use `@testing-library/react`'s `renderHook` and `act` to test the hook.
- Write all six test cases from behavior block above.
- Run tests — they MUST fail (hook doesn't exist yet).

**GREEN** — Create `src/lib/firebase.ts`:
- Single `initializeApp()` call using `import.meta.env.VITE_FIREBASE_*` variables
- Export `firebaseApp`
- `getModelName()`: async function that calls `fetchAndActivate` in try/catch (silently falls back), returns `getValue(remoteConfig, "model_name").asString()`
- Set `remoteConfig.settings.minimumFetchIntervalMillis = 3_600_000` (1 hour)
- Set `remoteConfig.defaultConfig = { model_name: "gemini-2.5-flash" }`
- `createModel(modelName)`: returns `getGenerativeModel(ai, { model: modelName, systemInstruction: ..., generationConfig: { temperature: 1.4, maxOutputTokens: 120 } })`
- System instruction (exact text from RESEARCH.md Pattern 1): Grand Compliment Oracle personality, absurdly dramatic tone, 2-3 sentences, weave name in, universal compliments only
- `ai` instance: `getAI(firebaseApp, { backend: new GoogleAIBackend() })`

Create `src/hooks/useCompliment.ts`:
- Export `ComplimentState` discriminated union type: idle | generating | result (with name + compliment) | error
- Export `useCompliment()` hook returning `{ state: ComplimentState, generate: (name: string) => Promise<void> }`
- Use `useState<ComplimentState>({ status: "idle" })`
- Use `useRef(false)` for `isFlyingRef` debounce guard
- `generate` wrapped in `useCallback` with empty dependency array
- Inside generate: check isFlyingRef, set generating, try { getModelName -> createModel -> generateContent -> result } catch { error } finally { isFlyingRef = false }
- Prompt: `Generate a compliment for: ${name}`

Run tests — all six MUST pass.
  </action>
  <verify>
    <automated>cd /Volumes/x10/CODE/FunProjectSong && npx vitest run src/hooks/useCompliment.test.ts --reporter=verbose</automated>
  </verify>
  <done>All 6 hook tests pass. useCompliment transitions through idle/generating/result/error correctly. Debounce guard prevents double-fire. Firebase initialized once as singleton. Model name resolved via Remote Config with fallback.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` — all tests green
- `npx tsc --noEmit` — no TypeScript errors
- `.env.example` contains all 6 VITE_FIREBASE_ keys
- `src/lib/firebase.ts` exports firebaseApp, getModelName, createModel
- `src/hooks/useCompliment.ts` exports useCompliment, ComplimentState type
</verification>

<success_criteria>
- useCompliment hook manages four-state machine (idle/generating/result/error)
- Firebase singleton prevents duplicate init
- Remote Config fetches model name with silent fallback
- Debounce ref prevents double-fire
- All unit tests pass with mocked Firebase
- No real Firebase credentials in repo (only .env.example with empty values)
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-foundation/03-01-SUMMARY.md`
</output>
