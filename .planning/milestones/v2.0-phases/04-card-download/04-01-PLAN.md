---
phase: 04-card-download
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Card/ComplimentCard.tsx
  - src/hooks/useCompliment.ts
  - src/hooks/useCompliment.test.ts
  - src/hooks/useTypewriter.ts
  - src/hooks/useTypewriter.test.ts
  - src/lib/downloadCard.ts
  - src/lib/downloadCard.test.ts
  - package.json
autonomous: true
requirements: [DWNL-01, DWNL-02, DWNL-03, GEN-03]

must_haves:
  truths:
    - "downloadCard utility calls document.fonts.ready before capturing"
    - "downloadCard utility passes pixelRatio >= 2 to toPng"
    - "downloadCard utility calls getFontEmbedCSS and passes result to toPng"
    - "downloadCard utility triggers browser download via anchor click"
    - "useCompliment hook streams chunks via generateContentStream"
    - "useCompliment state machine includes streaming status between generating and result"
    - "useTypewriter hook reveals characters one at a time from a growing source string"
    - "ComplimentCard accepts ref prop (React 19 style, no forwardRef)"
  artifacts:
    - path: "src/lib/downloadCard.ts"
      provides: "DOM-to-PNG download utility"
      exports: ["downloadCard"]
    - path: "src/lib/downloadCard.test.ts"
      provides: "Unit tests for downloadCard (mocked html-to-image)"
    - path: "src/hooks/useTypewriter.ts"
      provides: "Hook for character-by-character text reveal"
      exports: ["useTypewriter"]
    - path: "src/hooks/useTypewriter.test.ts"
      provides: "Unit tests for useTypewriter hook"
    - path: "src/hooks/useCompliment.ts"
      provides: "Updated hook with streaming state"
    - path: "src/hooks/useCompliment.test.ts"
      provides: "Extended tests covering streaming transitions"
    - path: "src/components/Card/ComplimentCard.tsx"
      provides: "Card with ref prop for DOM capture"
  key_links:
    - from: "src/lib/downloadCard.ts"
      to: "html-to-image"
      via: "import { toPng, getFontEmbedCSS }"
      pattern: "toPng.*pixelRatio"
    - from: "src/hooks/useCompliment.ts"
      to: "firebase/ai"
      via: "generateContentStream + for-await"
      pattern: "generateContentStream"
---

<objective>
Build all Phase 4 building blocks: downloadCard utility, useTypewriter hook, streaming useCompliment hook, and ref-enabled ComplimentCard.

Purpose: These are the four independent pieces that Plan 02 wires together in MainScreen. Building and testing them first prevents the "scavenger hunt" problem — Plan 02 receives fully tested contracts.

Output: Four tested modules ready for MainScreen integration.
</objective>

<execution_context>
@/Volumes/x10/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/x10/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-card-download/04-RESEARCH.md

<interfaces>
<!-- Current ComplimentCard interface (will be extended with ref) -->
From src/components/Card/ComplimentCard.tsx:
```typescript
interface ComplimentCardProps {
  name: string;
  compliment: string;
}
export function ComplimentCard({ name, compliment }: ComplimentCardProps) { ... }
```

From src/hooks/useCompliment.ts:
```typescript
export type ComplimentState =
  | { status: 'idle' }
  | { status: 'generating' }
  | { status: 'result'; name: string; compliment: string }
  | { status: 'error' };
export function useCompliment(): { state: ComplimentState; generate: (name: string) => Promise<void> }
```

From src/lib/firebase.ts:
```typescript
export function createModel(modelName: string): GenerativeModel;
// createModel returns an object with .generateContent() — will also have .generateContentStream()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install html-to-image, add ref to ComplimentCard, create downloadCard utility and useTypewriter hook with tests</name>
  <files>package.json, src/components/Card/ComplimentCard.tsx, src/lib/downloadCard.ts, src/lib/downloadCard.test.ts, src/hooks/useTypewriter.ts, src/hooks/useTypewriter.test.ts</files>
  <behavior>
    - downloadCard.test.ts: calls document.fonts.ready before toPng
    - downloadCard.test.ts: passes pixelRatio >= 2 (Math.max(2, window.devicePixelRatio)) to toPng
    - downloadCard.test.ts: calls getFontEmbedCSS and passes result as fontEmbedCSS option to toPng
    - downloadCard.test.ts: creates an anchor element, sets .download and .href, calls .click()
    - downloadCard.test.ts: if getFontEmbedCSS throws, downloadCard still completes (try/catch fallback)
    - useTypewriter.test.ts: returns empty string when input text is empty
    - useTypewriter.test.ts: after providing text="Hello", returned string grows character by character over time
    - useTypewriter.test.ts: when text grows (simulating streaming chunks), new characters are revealed incrementally
  </behavior>
  <action>
    1. Run `npm install --legacy-peer-deps html-to-image` to add the dependency.

    2. Update ComplimentCard to accept a `ref` prop (React 19 style — plain prop, NOT forwardRef):
       - Add `ref?: Ref<HTMLDivElement>` to ComplimentCardProps interface
       - Pass ref to the outer `<div>`
       - Import `Ref` type from 'react'

    3. Write downloadCard.test.ts FIRST (RED):
       - Mock `html-to-image` module: `vi.mock('html-to-image', () => ({ toPng: vi.fn(), getFontEmbedCSS: vi.fn() }))`
       - Mock `document.fonts` as `{ ready: Promise.resolve() }`
       - Mock `document.createElement` to return a spy anchor element for 'a' tag creation
       - Test: downloadCard calls getFontEmbedCSS, then toPng with pixelRatio >= 2 and fontEmbedCSS, then anchor.click()
       - Test: if getFontEmbedCSS rejects, toPng is still called (without fontEmbedCSS) and anchor.click() fires
       - Run `npm test -- src/lib/downloadCard.test.ts` — must FAIL (downloadCard.ts doesn't exist)

    4. Implement downloadCard.ts (GREEN):
       - Follow the exact pattern from 04-RESEARCH.md Pattern 4
       - Export `async function downloadCard(node: HTMLElement, filename = 'my-boost.png')`
       - Steps: await document.fonts.ready -> try getFontEmbedCSS catch -> toPng with pixelRatio Math.max(2, window.devicePixelRatio ?? 2) -> create anchor, set .download, .href, .click()
       - Run `npm test -- src/lib/downloadCard.test.ts` — must PASS

    5. Write useTypewriter.test.ts FIRST (RED):
       - This is a React hook, so use `renderHook` from @testing-library/react
       - Use `vi.useFakeTimers()` and `vi.advanceTimersByTime(ms)` to control setInterval timing
       - Test: `useTypewriter('')` returns empty string
       - Test: `useTypewriter('Hello')` — initially returns empty, after advancing 5*18ms returns "Hello"
       - Test: rerender with text growing from "He" to "Hello" — after advancing timers, new characters appear
       - Run `npm test -- src/hooks/useTypewriter.test.ts` — must FAIL

    6. Implement useTypewriter.ts (GREEN):
       - Export `function useTypewriter(text: string, speed = 18): string`
       - Uses useState for displayed string, useEffect with setInterval at `speed` ms per character
       - When `text` grows beyond `displayed.length`, types remaining characters one at a time
       - Clears interval on cleanup and when caught up
       - Run `npm test -- src/hooks/useTypewriter.test.ts` — must PASS

    7. Run full test suite: `npm test` — all tests pass (existing + new).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    - html-to-image installed in package.json
    - ComplimentCard accepts ref prop (React 19 style)
    - downloadCard.ts exports downloadCard function that calls fonts.ready, getFontEmbedCSS, toPng with pixelRatio >= 2, and triggers anchor download
    - useTypewriter hook returns characters incrementally from a growing text input
    - All new tests pass, all existing tests still pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update useCompliment hook to use generateContentStream with streaming state</name>
  <files>src/hooks/useCompliment.ts, src/hooks/useCompliment.test.ts</files>
  <behavior>
    - Existing tests still pass (idle, result, error, debounce, regenerate) after updating mocks from generateContent to generateContentStream
    - New test: generate("Alice") transitions through generating -> streaming (with partial text) -> result
    - New test: if stream throws mid-way, state transitions to error
  </behavior>
  <action>
    1. EXTEND useCompliment.test.ts FIRST (RED):
       - Replace `mockGenerateContent` with `mockGenerateContentStream` throughout
       - Update the firebase/ai mock: `getGenerativeModel` returns `{ generateContentStream: mockGenerateContentStream }` instead of `{ generateContent: mockGenerateContent }`
       - Create an async generator helper to simulate streaming chunks:
         ```typescript
         async function* fakeStream(chunks: string[]) {
           for (const chunk of chunks) {
             yield { text: () => chunk };
           }
         }
         ```
       - Update existing test mocks: `mockGenerateContentStream.mockResolvedValue({ stream: fakeStream(['mocked compliment']) })` (single chunk to preserve existing test behavior)
       - New test: "generate transitions through streaming to result with accumulated chunks" — mock returns `{ stream: fakeStream(['Hello ', 'world!']) }`, verify final state is `{ status: 'result', name: 'Alice', compliment: 'Hello world!' }`
       - New test: "if stream throws mid-way, state transitions to error" — create a stream that yields one chunk then throws
       - Run `npm test -- src/hooks/useCompliment.test.ts` — must FAIL (hook still uses generateContent)

    2. Update useCompliment.ts (GREEN):
       - Add `streaming` to ComplimentState union: `| { status: 'streaming'; name: string; compliment: string }`
       - Replace `model.generateContent(prompt)` with `model.generateContentStream(prompt)`
       - After setState generating: `const result = await model.generateContentStream(prompt)`
       - Enter `for await (const chunk of result.stream)` loop
       - Accumulate text: `accumulated += chunk.text()`
       - setState streaming with partial text after each chunk: `setState({ status: 'streaming', name, compliment: accumulated })`
       - After loop completes: `setState({ status: 'result', name, compliment: accumulated.trim() })`
       - Keep the try/catch error handling and debounce logic intact
       - Run `npm test -- src/hooks/useCompliment.test.ts` — must PASS

    3. Run full test suite: `npm test` — all tests pass.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    - ComplimentState type includes 'streaming' status
    - useCompliment uses generateContentStream and accumulates chunks
    - State transitions: idle -> generating -> streaming (partial text updates) -> result
    - Error during streaming transitions to error state
    - Debounce still works
    - All existing tests updated and passing, new streaming tests passing
  </done>
</task>

</tasks>

<verification>
1. `npm test` — all tests pass (existing + downloadCard + useTypewriter + streaming useCompliment)
2. No TypeScript errors: `npx tsc --noEmit`
3. ComplimentCard accepts ref prop without forwardRef wrapper
</verification>

<success_criteria>
- html-to-image installed
- downloadCard utility tested and working (mocked)
- useTypewriter hook tested and working
- useCompliment streams chunks via generateContentStream
- ComplimentCard exposes ref for DOM capture
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/04-card-download/04-01-SUMMARY.md`
</output>
