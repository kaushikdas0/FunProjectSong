---
phase: 01-design-language-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vite.config.ts
  - package.json
  - src/main.tsx
  - src/App.tsx
  - src/dls/tokens.css
  - src/dls/typography.css
  - src/dls/index.css
  - src/screens/MainScreen.tsx
  - tsconfig.json
  - tsconfig.app.json
  - index.html
autonomous: true
requirements:
  - DLS-01
  - DLS-02

must_haves:
  truths:
    - "All color tokens (blue, cream, coral, text) are available as both Tailwind utility classes and CSS custom properties"
    - "Caveat font loads from self-hosted files (no Google Fonts CDN requests) at weights 400, 600, 700"
    - "The dev server starts and renders a page with Caveat font and DLS token colors visible"
  artifacts:
    - path: "src/dls/tokens.css"
      provides: "All DLS color, font, radius, and spacing tokens via @theme block"
      contains: "@theme"
    - path: "src/dls/typography.css"
      provides: "Type scale classes (display through label) using Caveat"
      contains: ".text-display"
    - path: "src/dls/index.css"
      provides: "Single CSS entry point importing Tailwind and DLS layers"
      contains: "@import"
    - path: "src/main.tsx"
      provides: "App entry importing @fontsource/caveat weights and DLS CSS"
      contains: "@fontsource/caveat"
  key_links:
    - from: "src/main.tsx"
      to: "src/dls/index.css"
      via: "CSS import"
      pattern: "import.*dls/index"
    - from: "src/dls/index.css"
      to: "src/dls/tokens.css"
      via: "@import"
      pattern: "@import.*tokens"
    - from: "src/main.tsx"
      to: "@fontsource/caveat"
      via: "npm import"
      pattern: "@fontsource/caveat"
---

<objective>
Scaffold the React + Vite + Tailwind v4 project and create the complete DLS token system (colors, typography, fonts).

Purpose: Establish the project foundation and all visual tokens that every component in Phase 1 and Phase 2 will consume. Self-hosted Caveat font is critical for future card PNG export.

Output: A running Vite dev server with all DLS tokens available as Tailwind utilities and CSS custom properties. Caveat font rendering at all type scale steps.
</objective>

<execution_context>
@/Volumes/x10/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/x10/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-language-system/01-CONTEXT.md
@.planning/phases/01-design-language-system/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite project and install all dependencies</name>
  <files>package.json, vite.config.ts, tsconfig.json, tsconfig.app.json, index.html, src/main.tsx, src/App.tsx</files>
  <action>
1. Scaffold the project using Vite's react-ts template. Since we are already in the project root (`/Volumes/x10/CODE/FunProjectSong`), scaffold into a temp directory then move files up, OR scaffold in-place if the tool supports it. The final result must have `package.json`, `vite.config.ts`, `src/main.tsx`, etc. at the project root — NOT in a subdirectory.

2. Install core dependencies:
   ```
   npm install tailwindcss @tailwindcss/vite
   npm install @fontsource/caveat
   npm install pixelarticons
   npm install react-router-dom
   ```

3. Configure `vite.config.ts` with React and Tailwind v4 plugins:
   ```typescript
   import { defineConfig } from 'vite';
   import react from '@vitejs/plugin-react';
   import tailwindcss from '@tailwindcss/vite';

   export default defineConfig({
     plugins: [
       react(),
       tailwindcss(),
     ],
   });
   ```

4. Update `src/main.tsx` to import Caveat font weights and DLS CSS:
   ```typescript
   import '@fontsource/caveat/400.css';
   import '@fontsource/caveat/600.css';
   import '@fontsource/caveat/700.css';
   import './dls/index.css';
   import { StrictMode } from 'react';
   import { createRoot } from 'react-dom/client';
   import App from './App';

   createRoot(document.getElementById('root')!).render(
     <StrictMode><App /></StrictMode>
   );
   ```

5. Create a minimal `src/App.tsx` that renders a placeholder `MainScreen`:
   ```tsx
   import MainScreen from './screens/MainScreen';

   export default function App() {
     return <MainScreen />;
   }
   ```

6. Create `src/screens/MainScreen.tsx` as a simple placeholder:
   ```tsx
   export default function MainScreen() {
     return (
       <div className="min-h-screen bg-cream-300 flex items-center justify-center">
         <h1 className="font-caveat text-4xl text-text-primary">
           EgoBoost 3000
         </h1>
       </div>
     );
   }
   ```

IMPORTANT: Do NOT create a `tailwind.config.js` or `tailwind.config.ts` file. Tailwind v4 uses CSS-only config via @theme.

Delete any Vite scaffold boilerplate that is not needed (src/App.css, src/index.css, assets/react.svg, etc.).
  </action>
  <verify>
    <automated>cd /Volumes/x10/CODE/FunProjectSong && npx tsc --noEmit 2>&1 | head -20 && echo "---" && node -e "const pkg = require('./package.json'); const deps = {...pkg.dependencies}; console.log('tailwindcss:', !!deps.tailwindcss, '@fontsource/caveat:', !!deps['@fontsource/caveat'], 'pixelarticons:', !!deps.pixelarticons, 'react-router-dom:', !!deps['react-router-dom'])"</automated>
  </verify>
  <done>Project scaffolded at repo root with Vite + React + TypeScript. All dependencies installed (tailwindcss, @tailwindcss/vite, @fontsource/caveat, pixelarticons, react-router-dom). vite.config.ts has React and Tailwind plugins. main.tsx imports Caveat font weights. No tailwind.config.js exists. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create DLS token system and typography scale</name>
  <files>src/dls/tokens.css, src/dls/typography.css, src/dls/index.css</files>
  <action>
1. Create `src/dls/tokens.css` with the complete @theme block. Use the exact token values from RESEARCH.md:

```css
@theme {
  /* Primary blue (periwinkle) */
  --color-blue-50:  #F3F6FB;
  --color-blue-100: #E6EDF6;
  --color-blue-200: #CCDAED;
  --color-blue-300: #B3C8E4;
  --color-blue-400: #9EB1CF;
  --color-blue-500: #7A97BE;
  --color-blue-600: #5B7CAC;
  --color-blue-700: #3D5A8A;

  /* Background/base (blush cream) */
  --color-cream-50:  #FFFCFA;
  --color-cream-100: #FFF8F5;
  --color-cream-200: #FFF4EF;
  --color-cream-300: #FFF0EC;
  --color-cream-400: #F8E8E2;
  --color-cream-500: #F0DDD6;

  /* Accent coral (buttons, CTAs) */
  --color-coral-300: #F5C4BA;
  --color-coral-400: #EEB4A8;
  --color-coral-500: #E8A598;
  --color-coral-600: #D98A7A;
  --color-coral-700: #C06E5E;

  /* Text colors */
  --color-text-primary:   #5A4A6F;
  --color-text-secondary: #8B7BA3;
  --color-text-muted:     #A99BBF;
  --color-text-light:     #C4A8B0;

  /* Typography */
  --font-caveat: 'Caveat', cursive;

  /* Border radii */
  --radius-card:   20px;
  --radius-button: 12px;
  --radius-input:  10px;
  --radius-swatch:  8px;

  /* Spacing (DLS-specific) */
  --spacing-card-pad-x: 36px;
  --spacing-card-pad-y: 40px;
}
```

2. Create `src/dls/typography.css` with the full type scale. All classes use `font-family: var(--font-caveat)`. Scale from RESEARCH.md:
   - `.text-display`: 3rem/700/1.2
   - `.text-heading-xl`: 2.25rem/700/1.3
   - `.text-heading-lg`: 1.875rem/600/1.3
   - `.text-heading-md`: 1.5rem/600/1.4
   - `.text-body-lg`: 1.25rem/400/1.6
   - `.text-body`: 1.125rem/400/1.6
   - `.text-body-sm`: 1rem/400/1.6
   - `.text-label`: 0.875rem/400/1.5 (minimum legible Caveat size)

3. Create `src/dls/index.css` as the single CSS entry point:
```css
@import "tailwindcss";
@import "./tokens.css";
@import "./typography.css";
```

IMPORTANT: All tokens MUST be inside @theme, not in :root. Tokens in :root will not generate Tailwind utility classes.
  </action>
  <verify>
    <automated>cd /Volumes/x10/CODE/FunProjectSong && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>Three DLS CSS files exist. tokens.css contains @theme block with all color, font, radius, and spacing tokens. typography.css contains 8 type scale classes using Caveat. index.css imports Tailwind and both DLS files. Vite build completes successfully, confirming Tailwind v4 processes the @theme block without errors.</done>
</task>

</tasks>

<verification>
1. `npm run dev` starts without errors
2. Visiting `http://localhost:5173` shows "EgoBoost 3000" in Caveat font on a cream background
3. Browser DevTools computed styles show Caveat as the rendered font (not system fallback)
4. No `tailwind.config.js` or `tailwind.config.ts` exists in project root
5. No requests to `fonts.googleapis.com` in Network tab
</verification>

<success_criteria>
- Vite dev server runs and renders the MainScreen placeholder
- Caveat font renders from self-hosted files (no CDN)
- All DLS tokens are defined in @theme and generate Tailwind utility classes
- TypeScript compiles without errors
- Vite production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-language-system/01-01-SUMMARY.md`
</output>
