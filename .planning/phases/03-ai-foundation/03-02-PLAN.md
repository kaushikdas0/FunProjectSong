---
phase: 03-ai-foundation
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/screens/MainScreen.tsx
  - src/screens/MainScreen.test.tsx
autonomous: false
requirements: [GEN-01, GEN-03, GEN-04, GEN-05]
must_haves:
  truths:
    - "User can type a name into an auto-focused input field"
    - "Generate button is disabled until name has 3+ characters"
    - "Tapping Boost Me triggers compliment generation and button shows Boosting..."
    - "After result, card appears with name and compliment; button shows Boost Again"
    - "On error, a playful message and Try Again button replace the card area"
    - "Name stays editable after compliment shows; card persists during edits"
    - "Compliment text appears all at once (non-streaming prerequisite for Phase 4 typewriter)"
  artifacts:
    - path: "src/screens/MainScreen.tsx"
      provides: "Full generate flow UI — input, button, card, error state"
      min_lines: 60
    - path: "src/screens/MainScreen.test.tsx"
      provides: "Integration tests for button states and error rendering"
      min_lines: 40
  key_links:
    - from: "src/screens/MainScreen.tsx"
      to: "src/hooks/useCompliment.ts"
      via: "const { state, generate } = useCompliment()"
      pattern: "useCompliment\\(\\)"
    - from: "src/screens/MainScreen.tsx"
      to: "src/components/Card/ComplimentCard.tsx"
      via: "<ComplimentCard name={...} compliment={...} />"
      pattern: "ComplimentCard"
---

<objective>
Wire MainScreen with name input, generate/regenerate button, ComplimentCard display, and error state UI. This is the user-facing surface of the AI foundation.

Purpose: Connects the useCompliment hook (Plan 01) to the visual interface. After this plan, a user can enter their name, tap "Boost Me", and see a real AI-generated compliment on the card.

Output: Fully functional MainScreen with all generate flow states, integration tests, human verification.
</objective>

<execution_context>
@/Volumes/x10/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/x10/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-foundation/03-CONTEXT.md
@.planning/phases/03-ai-foundation/03-RESEARCH.md
@.planning/phases/03-ai-foundation/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — useCompliment hook contract -->
From src/hooks/useCompliment.ts:
```typescript
export type ComplimentState =
  | { status: "idle" }
  | { status: "generating" }
  | { status: "result"; name: string; compliment: string }
  | { status: "error" };

export function useCompliment(): {
  state: ComplimentState;
  generate: (name: string) => Promise<void>;
};
```

<!-- Existing ComplimentCard — receives name + compliment -->
From src/components/Card/ComplimentCard.tsx:
```typescript
interface ComplimentCardProps {
  name: string;
  compliment: string;
}
export function ComplimentCard({ name, compliment }: ComplimentCardProps): JSX.Element;
```

<!-- DLS tokens available (from Phase 1) -->
<!-- Tailwind classes: bg-cream-50, bg-cream-300, text-text-primary, text-text-secondary, text-text-muted -->
<!-- Font: font-caveat for display text -->
<!-- Colors: coral-400 (accent), cream-* (backgrounds), plum-* (if needed) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MainScreen with full generate flow and integration tests</name>
  <files>src/screens/MainScreen.tsx, src/screens/MainScreen.test.tsx</files>
  <behavior>
    - Test: Input renders with auto-focus and playful placeholder text
    - Test: "Boost Me" button is disabled when input has fewer than 3 characters
    - Test: "Boost Me" button is enabled when input has 3+ characters
    - Test: During generating state, button shows "Boosting..." and is disabled
    - Test: After result state, button shows "Boost Again"
    - Test: Error state renders playful error message and "Try Again" button (no raw error text)
    - Test: ComplimentCard renders with name and compliment when in result state
  </behavior>
  <action>
**RED first** — Write `src/screens/MainScreen.test.tsx`:
- Mock `src/hooks/useCompliment` module — control the returned `state` and `generate` function
- Use `@testing-library/react` `render`, `screen`, `fireEvent` / `userEvent`
- Write all seven test cases from the behavior block
- For the "auto-focus" test: check that the input element has focus after render
- For button state tests: mock useCompliment returning different `state.status` values and assert button text/disabled attribute
- For error test: mock `{ status: "error" }` and assert the playful message text is visible and no string like "Error" or "500" appears
- Run tests — MUST fail (MainScreen is currently a stub)

**GREEN** — Rewrite `src/screens/MainScreen.tsx` (completely replace the stub):

Layout structure (use DLS tokens and Tailwind):
- Full-height centered layout: `min-h-screen bg-cream-300 flex flex-col items-center justify-center px-4`
- Title: "EgoBoost 3000" in font-caveat, large size, text-text-primary, above the input
- Input: styled text input with playful placeholder (Claude's discretion, e.g., "Who deserves a cosmic boost?"), auto-focus via useRef + useEffect, max-length 40, min-length validation (3 chars) drives button disabled state. Style: rounded, padding, bg-cream-50, border-cream-400, font-caveat for typed text, generous size for mobile tapping
- Button: "Boost Me" / "Boosting..." / "Boost Again" based on state. Disabled when name < 3 chars OR generating. Style: bg-coral-400 text-cream-50, rounded, hover state, disabled opacity. When generating, show a simple CSS spinner (small rotating border circle) inline before "Boosting..." text
- Card area (below button): conditionally renders ComplimentCard when `state.status === "result"` passing `state.name` and `state.compliment`
- Error area (replaces card area): conditionally renders when `state.status === "error"`. Shows playful in-character message (Claude's discretion, e.g., "Even the universe needs a coffee break. Try again?") and a "Try Again" button that calls `generate(name.trim())`
- Card area stays hidden during idle and generating states (per locked decision: "card area stays hidden until ready")
- Name input stays editable in all states; card persists when name is edited (no state reset on onChange)

Wire up:
- `const [name, setName] = useState("")`
- `const { state, generate } = useCompliment()`
- `const inputRef = useRef<HTMLInputElement>(null)` with `useEffect(() => inputRef.current?.focus(), [])`
- Button onClick: `generate(name.trim())`
- Input onChange: `setName(e.target.value)` only — no state reset

CSS spinner (for loading state): Create with Tailwind utility classes — a small `w-4 h-4 border-2 border-cream-50 border-t-transparent rounded-full animate-spin` span inline before "Boosting..." text. Define the spin animation in the component file or use Tailwind's built-in `animate-spin`.

Run tests — all seven MUST pass.
  </action>
  <verify>
    <automated>cd /Volumes/x10/CODE/FunProjectSong && npx vitest run src/screens/MainScreen.test.tsx --reporter=verbose</automated>
  </verify>
  <done>All 7 MainScreen integration tests pass. Input auto-focuses. Button shows correct text per state. Card renders on result. Error shows playful message with retry. No raw API errors exposed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete AI generation flow end-to-end</name>
  <files>n/a</files>
  <action>
Human verifies the complete generate flow with a real Firebase project and Gemini API.

Prerequisites: Firebase project with AI Logic enabled and a `.env` file with real Firebase config values (see `.env.example`).

Steps to verify:
1. Start dev server: `npm run dev`
2. Open the app in browser (likely http://localhost:5173)
3. Verify: Input field is auto-focused with playful placeholder text
4. Type "Al" (2 chars) — verify "Boost Me" button is disabled
5. Type "Ali" (3 chars) — verify "Boost Me" button becomes enabled
6. Tap "Boost Me" — verify:
   - Button changes to "Boosting..." with a spinner
   - After a moment, a compliment card appears with the name and an absurdly dramatic AI-generated compliment
   - Button now says "Boost Again"
7. Edit the name to "Sarah" — verify card stays visible (does not disappear)
8. Tap "Boost Again" — verify a NEW compliment appears (different from the first)
9. Test error state: Disconnect internet or use an invalid Firebase project, tap generate — verify a playful error message appears (not a raw error) with a "Try Again" button
10. Verify: No raw API errors or stack traces are ever shown to the user
  </action>
  <verify>Human visual confirmation of all 10 steps above</verify>
  <done>User confirms: generate flow works end-to-end, error handling is friendly, no raw errors shown, button states cycle correctly, card persists during name edits.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` — all tests green (both hook and MainScreen tests)
- `npx tsc --noEmit` — no TypeScript errors
- Visual: Complete generate flow works with real Gemini API
- Visual: Error states show playful message, no raw errors
- Visual: Button labels cycle correctly (Boost Me -> Boosting... -> Boost Again)
</verification>

<success_criteria>
- User can type a name, tap Generate, and see a real Gemini-generated compliment on the card
- User can tap Boost Again and receive a different compliment
- Error state shows friendly message with retry — no raw API errors
- Generate button disabled until 3+ chars and during generation
- Input auto-focuses on load
- Compliment appears all at once (non-streaming, prerequisite for Phase 4 typewriter)
- All automated tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-foundation/03-02-SUMMARY.md`
</output>
